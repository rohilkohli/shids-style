// ============================================================================
// Admin Panel Constants
// ============================================================================

/**
 * Stock threshold below which a product is considered "low stock"
 */
export const LOW_STOCK_THRESHOLD = 5;

/**
 * Number of steps in the product creation wizard
 */
export const PRODUCT_WIZARD_STEPS = 5;

/**
 * Default pagination page size
 */
export const DEFAULT_PAGE_SIZE = 10;

/**
 * Maximum file size for image uploads (in bytes)
 * 5MB default
 */
export const MAX_IMAGE_SIZE = 5 * 1024 * 1024;

/**
 * Supported image formats for product images
 */
export const SUPPORTED_IMAGE_FORMATS = ["image/jpeg", "image/png", "image/webp", "image/gif"];
